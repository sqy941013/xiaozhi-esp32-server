# React 管理前端迁移与交付记录

## 目标

使用 React、TypeScript、Vite、Tailwind CSS 和 shadcn/ui 完成管理台全模块重写。React 前端位于 `main/manager-web-next`；迁移期间通过独立镜像和端口验收，阶段 7 后已成为正式 `Dockerfile-web` 和源码 Compose 的唯一前端源码。

## 兼容性约束

- 保持 `/xiaozhi` API 前缀和 Spring Boot `Result<T>` 响应语义。
- 保持现有六种语言：`zh-CN`、`zh-TW`、`en`、`de`、`pt-BR`、`vi`。
- 所有业务路由默认需要登录，公开路由使用显式允许列表。
- 不自动重试 POST、PUT、PATCH、DELETE 等变更请求。
- 上传、下载、音频和二进制响应必须通过真实浏览器验收。
- 智能体快照恢复必须保留原子状态令牌、二次确认和敏感字段脱敏。
- Vue 2 前端仅在 React 全模块验收、正式切流和升级/回滚演练完成后删除；旧 `local` 镜像仍可作为运行时回滚点。

## 技术方案

| 层级 | 选型 | 约束 |
| --- | --- | --- |
| 运行时 | React 19、TypeScript 严格模式 | 禁止用 `any` 绕过业务模型，页面按业务域懒加载 |
| 构建与样式 | Vite 8、Tailwind CSS 4 | Tailwind 只扫描 `src`，保证测试前后产物可重复 |
| 组件 | shadcn/ui、Radix UI、Lucide | 组件源码归仓库管理，不依赖黑盒组件包 |
| 路由 | React Router Data Mode | 公共路由、登录路由、权限路由分层定义 |
| 服务端状态 | TanStack Query | 查询可有限重试，所有变更请求默认不重试 |
| HTTP | Axios、Springdoc OpenAPI 生成类型 | 统一处理 `Result<T>`、令牌、业务错误和二进制响应 |
| 国际化 | i18next、react-i18next | 六种语言保持同一键结构，CI 检查缺失键 |
| 测试 | Vitest、Testing Library、Playwright | 单元测试与浏览器测试目录隔离 |
| 交付 | Node 22 多阶段构建、Nginx、JRE 21 | 预览镜像保持独立；正式镜像组合 React、manager-api、健康检查和 API 反向代理 |

阶段 1–6 采用新旧前端并行目录和独立预览镜像：Vue 管理台使用 `18002`，React 预览使用 `18012`。阶段 7 在全部路由、权限、上传下载和高风险操作通过验收后，将正式 `Dockerfile-web` 和 `18002` 切换为 React；预览镜像继续保留用于独立验证。

Docker 构建默认通过 `https://registry.npmmirror.com` 下载 pnpm 依赖，并保留 BuildKit 内容缓存；如需切换源，可传入 `--build-arg PNPM_REGISTRY=<registry>`。HTTP/HTTPS 代理继续通过 Docker 标准构建参数注入，不写入镜像层或仓库配置。

## 实施阶段

| 阶段 | 范围 | 合并条件 |
| --- | --- | --- |
| 1. 工程基础 | React/Vite/Tailwind/shadcn 基础、OpenAPI 类型、六语言、Docker、CI | 已合并（PR #2） |
| 2. 应用骨架 | 登录/注册/找回密码、令牌续用、权限守卫、菜单、整体布局、API 客户端 | 已合并（PR #3） |
| 3. 模型中心 | 模型配置、供应器管理、动态字段、TTS 音色与敏感配置 | 已合并（PR #4） |
| 4. 智能体与设备 | 首页、角色配置、模板、设备绑定、通讯录、声纹 | 已合并（PR #5） |
| 5. 知识与媒体 | 知识库、语音资源、声音克隆、OTA | 已合并（PR #6） |
| 6. 系统管理 | 用户、参数、字典、功能、替换词、服务端管理 | 已合并（PR #7） |
| 7. 切换与收尾 | Compose 改用本地 React 镜像、全路由回归、升级/回滚演练 | 已合并并完成正式切流（PR #8） |
| 8. 旧版清理 | 删除 Vue 2 源码与断链工具、修正回滚提示、同步中英文文档 | 本地验收通过，待 PR 合并 |

每个阶段使用独立分支和 PR；一个阶段合并并通过主分支 CI 后，才开始下一个阶段。这样每次回滚只影响一个业务域，也便于继续跟随上游仓库升级。

## 目录边界

```text
main/manager-web-next/
├── e2e/                 # Playwright 业务路径
├── nginx/               # 运行时代理和 SPA 配置
├── public/              # 设备主题生成器、协议页和 favicon 静态资产
├── scripts/             # OpenAPI 等生成脚本
└── src/
    ├── api/             # 生成契约、HTTP 客户端、业务 API
    ├── app/             # Provider、路由、权限和整体布局
    ├── components/ui/   # shadcn/ui 基础组件
    ├── features/        # 按业务域垂直切片
    ├── i18n/            # 语言资源和键校验
    ├── lib/             # 无业务状态的共享工具
    └── test/            # 测试初始化和夹具
```

## 路由迁移矩阵

| 业务域 | 旧版路由 | 风险 | 状态 |
| --- | --- | --- | --- |
| 认证 | `/`、`/login` | 高 | 已迁移并合并 |
| 认证 | `/register`、`/retrieve-password` | 中 | 已迁移并合并 |
| 应用骨架 | 全部业务路由、权限导航、响应式布局 | 高 | 已迁移并合并 |
| 智能体 | `/home`、`/role-config` | 极高 | 阶段 4 已迁移并验收 |
| 智能体 | `/agent-template-management`、`/template-quick-config` | 高 | 阶段 4 已迁移并验收 |
| 设备 | `/device-management`、`/address-book-management` | 高 | 阶段 4 已迁移并验收 |
| 模型 | `/model-config`、`/provider-management` | 高 | 阶段 3 已迁移并合并 |
| 知识库 | `/knowledge-base-management` | 高 | 阶段 5 已迁移并验收 |
| 语音 | `/voice-print` | 高 | 阶段 4 已迁移并验收 |
| 语音 | `/voice-resource-management` | 高 | 阶段 5 已迁移并验收 |
| 语音 | `/voice-clone-management` | 高 | 阶段 5 已迁移并验收 |
| OTA | `/ota-management` | 高 | 阶段 5 已迁移并验收 |
| 系统 | `/user-management`、`/params-management` | 中 | 阶段 6 已迁移并验收 |
| 系统 | `/dict-management`、`/feature-management` | 中 | 阶段 6 已迁移并验收 |
| 系统 | `/replacement-word-management` | 中 | 阶段 6 已迁移并验收 |
| 运维 | `/server-side-management` | 高 | 阶段 6 已迁移并验收 |

## 阶段 2 认证与权限约束

- 沿用 Vue 前端的 `token` JSON 存储格式，可直接续用已有登录态；损坏的令牌会被安全忽略。
- 登录、注册和找回密码继续使用后端要求的 SM2 C1C3C2 密文，明文密码不会进入请求体或日志。
- 前端使用已修复安全公告的 `sm-crypto 0.5.5`，并由 Java/Bouncy Castle 跨语言固定密文测试锁定 C1C3C2 兼容性。
- 所有业务路由默认受保护，只有 `/`、`/login`、`/register` 和 `/retrieve-password` 显式公开。
- 超级管理员菜单与路由同时校验 `superAdmin === 1`；音色、知识库、声纹和通讯录同时受公共功能开关控制。
- 登录前的跳转目标只接受站内绝对路径，拒绝 `//host` 和外部 URL，避免开放重定向。
- 401 会原子清除令牌和用户缓存并回到登录页；公共配置缓存独立保留，用于服务短暂不可达时展示。
- `Accept-Language` 与后端格式保持一致，英语发送 `en-US`，其余语言使用 BCP 47 格式。
- 查询最多有限重试；登录、注册、短信、重置密码和修改密码等变更请求均不自动重试。

## 阶段 3 模型中心约束

- 模型分类、搜索、分页和每页条数进入 URL 查询参数，刷新或分享链接后仍保留当前视图。
- 供应器字段定义驱动模型表单；布尔、数字、对象、数组和密码字段按类型读写，不再把 `0` 或 `false` 当成空值。
- 已保存密钥只显示“已安全保存”状态，输入框保持为空；未填写时请求体省略该字段，由后端保留原值。
- 切换供应器时后端从空配置合并，避免旧供应器密钥和私有字段进入新配置；文档链接和模型编码可正确更新。
- 默认模型使用明确的二次确认，不能被禁用或删除；批量删除自动排除默认模型。
- TTS 音色支持查询、试听、新增、修改和批量删除，并保留 FishSpeech/GPT-SoVITS 的参考音频字段。
- 文档链接只允许 HTTP/HTTPS，试听地址只额外允许同源绝对路径，拒绝危险协议和协议相对 URL。
- 六种语言的模型文案在迁移时由确定性脚本导入，再叠加 React 新交互文案；现作为 React 自有语言快照维护，测试检查每个语言包都包含完整工作流入口。
- 本机验收结果：ESLint、严格类型、26 项 Vitest、6 项 JDK 21 后端测试、11 项 Playwright、生产构建、Docker 健康检查和生产依赖审计全部通过。

## 阶段 4 智能体与设备约束

- 工作台支持按智能体名称或设备 MAC 查询、新建、配置、查看聊天记录和删除；删除必须手工输入完整智能体名称，禁止粘贴绕过确认。
- 角色配置覆盖 VAD、ASR、LLM、SLM、VLLM、Intent、Memory、TTS、音色、标签、替换词、插件函数和上下文源；`0`、`false` 等有效配置不会在保存时丢失。
- 选择无记忆模型时同步关闭聊天记录上报并清空摘要；快照恢复必须携带服务端返回的 `currentStateToken`，展示字段差异并经过二次确认。
- 设备支持验证码绑定、基于 `FIRMWARE_TYPE` 字典的手动添加、备注修改、自动升级开关和单个/批量解绑；连字符 MAC 会规范化为大写冒号格式。
- 通讯录聚合用户名下全部智能体设备，按主叫设备维护别名与呼叫权限，并对后端返回的 MAC 大小写差异进行归一化。
- 声纹使用真实的近期用户语音记录创建，可试听、编辑和删除；聊天记录支持会话分页、音频播放和带令牌的当前/历史下载。
- 功能开关路由在公共配置尚未加载时保持加载态，避免直接访问通讯录或声纹深链被错误重定向。
- 六种语言的智能体、设备、模板、快照、通讯录和声纹文案在迁移时从 Vue 语言包导入，并叠加 React 通用交互文案；现作为 React 自有语言快照维护。
- 本机验收结果：ESLint 零警告、严格类型检查、42 项 Vitest、16 项 Playwright、生产构建、Docker 镜像健康检查、SPA 深链检查和生产依赖审计全部通过。

## 阶段 5 知识与媒体约束

- 知识库支持创建、修改、启停和删除；文档支持查询、分页、批量删除、多文件上传、解析状态轮询、分段查看和检索测试。
- 文档上传限制为单次最多 32 个文件、单文件不超过 10 MB，并校验后端支持的扩展名；上传失败时保留未完成文件，便于继续重试。
- 解析进度直接使用列表接口返回的状态、进度和分段数，仅在存在运行中任务时每 5 秒刷新，避免旧版逐条查询造成的 N+1 请求。
- 声音复刻支持样本解码、波形预览、本地裁剪并重新编码为 WAV；有效时长限制为 8–60 秒，上传过程展示真实进度。
- 试听与下载先向后端申请一次性令牌，再生成受控 URL；OTA 下载通过原生 `download` 链接触发，不把令牌长期写入状态或日志。
- OTA 固件支持新增、修改、批量删除及 `.bin`/`.apk` 上传，单文件上限 100 MB；浏览器不手工设置 multipart boundary。
- 所有路径参数先编码，查询请求可有限重试，上传、解析、克隆、保存和删除等变更请求均只发送一次。
- 六种语言的知识库、声音复刻、音色资源和 OTA 文案在迁移时从 Vue 语言包导入，现作为 React 自有语言快照维护，并由测试校验关键工作流键完整。
- 本机验收结果：ESLint 零警告、严格类型检查、53 项 Vitest、20 项 Playwright、生产构建、Docker 镜像健康检查、SPA 深链检查和生产依赖审计全部通过。

## 阶段 6 系统管理约束

- 用户管理仅允许超级管理员访问，支持手机号查询、分页、单个或批量启停及删除；重置密码必须二次确认，并在独立对话框中展示和复制后端生成的新密码。
- 参数管理按字符串、数字、布尔值、数组和 JSON 严格解析；密钥、令牌和密码类参数默认遮罩，`max_tokens` 等普通配置不会被误判为凭据。
- 字典类型和字典数据采用双栏联动管理，新增、修改及批量删除分别调用后端既有接口；翻页、查询或切换类型时清除过期选择，避免误操作其他页面的数据。
- 替换词文件支持 UTF-8 TXT 导入、编辑、分页、批量删除及带令牌下载；每行必须恰有一个 `|`，最多 4000 条且不超过 1 MiB，下载文件名会过滤控制字符和路径字符。
- 服务端管理只向后端返回的 WebSocket 地址发送 `restart` 或 `update_config`，所有指令都需二次确认；请求窗口覆盖后端最长执行时间，并把 `false` 返回值视为失败。
- 功能配置不再硬编码参数 ID，而是按 `system-web.menu` 精确查询后更新；保存和重置都等待后端成功并刷新公共配置，同时保留未知功能、分组、顶层字段及未来元数据。
- 所有变更请求只发送一次；用户、参数、字典、替换词、服务端和功能配置的读取错误与部分失败均有明确反馈。
- 六种语言的系统管理文案在迁移时从 Vue 语言包导入，再叠加 React 专用交互文案；现作为 React 自有语言快照维护，测试校验六个完整工作流的关键翻译键。
- 本机验收结果：ESLint 零警告、严格类型检查、64 项 Vitest、26 项 Playwright、生产构建、Docker 镜像健康检查、六条 SPA 深链检查和生产依赖审计全部通过。

## 阶段 7 切换与交付约束

- 正式 `Dockerfile-web` 使用 Node 22/pnpm 构建 `manager-web-next`，继续在同一容器内运行 Java `manager-api`，因此 `/xiaozhi` 前缀、Compose 服务名、端口和上传目录保持兼容。
- 设备主题生成器、字体/唤醒词模型、用户协议、隐私政策和 favicon 已迁入 React 的 `public/`，由 Vite 原样复制；设备列表补回带编码设备 ID 的主题生成入口。
- Web 健康检查必须真实访问公开配置 API，而非只探测 Nginx；Java 或 Nginx 任一进程退出时，监督脚本会终止另一个进程并让容器退出。
- Spring 数据库和 Redis 配置只通过环境变量绑定，不再作为 Java 命令行参数出现；Nginx API 超时与前端最长请求窗口统一为 180 秒。
- `docker-compose.source.yml` 只构建本地业务代码，固定 MySQL 8.4 和 Redis 8.0，并允许通过 `.env` 配置源码、运行数据、镜像仓库、端口、国内镜像和 HTTP 代理。
- `source-deploy.sh` 在升级前生成 0600 权限的 MySQL 逻辑备份，以 UTC 时间和 Git 提交生成不可变镜像标签，只更新两个业务容器，等待健康后记录当前/上一个版本；失败自动恢复应用镜像，支持显式回滚且不自动覆盖数据库。
- 本地候选验收结果：ESLint 零警告、严格类型检查、65 项 Vitest、26 项 Vite 预览 Playwright、26 项正式 Nginx Playwright、双 Docker 镜像构建、真实 MySQL/Redis 健康检查、23 条 SPA 深链、生成器大文件、协议页、缓存头和进程监督全部通过。
- PR #8 的兼容契约、前端质量/E2E、预览镜像和正式镜像四项 CI 全部通过；合并提交为 `759eb574`。
- 正式环境已使用不可变标签 `react-20260814-759eb574` 在 `18002` 完成源码升级、旧 `local` 版本回滚和 React 再切换；25 条 SPA 深链、公开 API、生成器、协议页、两个设备端口及 26 项 Playwright 全部通过。
- 三次切换前均生成 0600 权限且可通过 `gzip -t` 的 MySQL 备份；MySQL/Redis 容器 ID、30 张业务表、表行估算和 Redis 键数在演练前后保持一致。
- 旧 Vue 源码在正式切流验证后移除；运行中的 React 镜像与保留的 `local` 回滚镜像均不依赖该目录。

## 阶段 8 旧版清理

- 删除 `main/manager-web` 下 187 个旧 Vue 2 跟踪文件及 npm/Webpack 依赖，共约 56,000 行；React 的设备主题生成器、协议页、字体和模型静态资源继续保留在 `main/manager-web-next/public/`。
- 删除四个依赖旧 Vue 语言包的迁移生成脚本及 package 命令；已导入的六语言资源改为 React 自有迁移快照，由现有 i18n 完整性测试继续约束。
- Java 智能体快照测试改为检查 React 角色配置页的统一保存路径；前端 API 测试明确验证 `tagNames` 与其他智能体字段通过同一 PUT 请求提交。
- 修复 `source-deploy.sh` 在状态交换后打印错误回滚标签的问题，把脚本入口封装为可测试函数，并新增无 Docker 副作用的 Shell 回归测试。
- 中文、英文技术说明和两份手工部署文档均更新为 Node 22、pnpm 11、React/Vite 路径和环境变量。
- 本机验收结果：Shell 回归、ESLint、严格类型、65 项 Vitest、65 项 Java 21 测试、双 Docker 镜像构建、真实 MySQL/Redis 候选健康检查和 26 项 Playwright 全部通过。

## 迁移前已确认的基线

- Vue 单文件组件：60 个。
- Vue 视图与组件代码：约 28,000 行。
- 现有前端测试：5 个文件。
- Springdoc OpenAPI 路径：127 条。
- 新前端 OpenAPI 类型来源：`/xiaozhi/v3/api-docs`。

迁移完成后，仓库中不再保留 Vue 2 应用及其 npm/Webpack 依赖；设备主题生成器等仍需兼容的独立静态应用已迁入 React `public/`。

## 阶段合并门槛

每个迁移阶段都必须通过：

1. ESLint 零警告。
2. TypeScript 严格类型检查。
3. Vitest 单元和组件测试。
4. Vite 生产构建。
5. Docker 镜像构建和健康检查。
6. 对应业务域的 Playwright 端到端测试。
7. PR 合并后才开始下一个业务域。
